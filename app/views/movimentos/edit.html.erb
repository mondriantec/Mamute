<h1>Movimento</h1>

<% form_for(@movimento) do |f| %>
  <%= f.error_messages %>

  <%= render :partial => "formulario", :locals => {:f => f} %>

  <p>
    <%= f.submit "Atualizar" %>
  </p>

<% end %>

<%= link_to 'Voltar', movimentos_path %>
